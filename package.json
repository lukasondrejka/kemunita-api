{
  "name": "kemunita-api",
  "version": "0.0.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --local",
    "dev-migrate": "wrangler d1 migrations apply kemunita_db --local",

    "build": "wrangler build",
    "publish": "wrangler publish"
  },
  "dependencies": {
    "hono": "^4.7.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250429.0",
    "wrangler": "^4.13.2"
  }
}
